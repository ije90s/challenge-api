# 2026-09-14 워크로그

## Phase 4 — 부하 테스트 시나리오 설계

### 분석 & 질문

- `refactor_plan.md` Phase 4의 첫 TODO("부하 테스트 시나리오 설계, 랭킹 조회/동시 참가·기록 갱신 우선")를 처리 대상으로 확인.
- 관련 코드 분석: `participation.service.ts`의 `getChallengeRank`(랭킹 조회, 정렬 방향과 인덱스 컬럼 순서 불일치 가능성 — `refactor_plan.md` §1-7), `ParticipationService.update`(락 없는 read-modify-write, `refactor_plan.md` §1-2에서 실측 필요로 이월된 항목), 인증 흐름(`user`/`auth` 컨트롤러), 엔드포인트 계약(`participation.controller.ts`, `challenge.controller.ts`).
- 리포지토리에 부하 테스트 도구(k6/artillery 등)가 아직 없음을 확인.
- **질문**: 이번 세션에서 "시나리오 문서만" 작성할지, "문서 + 실행 가능한 스크립트"까지 만들지 `AskUserQuestion`으로 확인 → **"시나리오 문서만 작성"** 선택 (도구 선정/스크립트/실측은 다음 TODO로 이월).

### 변경사항 요약

| 파일 | 내용 |
|---|---|
| `anything/load_test_plan.md` (신규) | 랭킹 조회 / 동시 참가·기록 갱신 시나리오 설계 문서 — 우선순위 근거, 대상 환경, 시딩 계획, 인증 처리, 시나리오 상세, 측정 지표, 성공/실패 기준 초안, 이번 문서에서 다루지 않는 것(도구/스크립트/실측) 명시 |
| `anything/refactor_plan.md` | Phase 4 첫 TODO 체크 + 완료 메모 |

### 커밋 해시

- 코드 커밋: `19148a8` (Phase 4: 부하 테스트 시나리오 설계 문서 작성)

### 검증 결과

- `npm run build` 통과.
- 소스 코드(`src/`) 변경 없는 문서 전용 작업이라 유닛 테스트/`/code-review`는 해당 사항 없음(생략 사유를 사용자에게 사전 고지).

### 예외사항 / 남겨둔 것

- 부하 테스트 실행 도구 선정(k6/artillery/autocannon 등), 실제 스크립트 작성, 실측, EXPLAIN 분석, 커서 기반 페이지네이션 검토, 원자적 갱신 방식 최종 결정은 모두 Phase 4의 다음 TODO들로 이월.
- 동시 기록 갱신(패턴 B: 동일 유저-챌린지 참가 로우에 대한 동시 PATCH)은 정상 트래픽 패턴이 아니라 포함 여부를 문서에서 "실행 단계에서 결정"으로 열어둠 — `load_test_plan.md` §5-2 참고.
