# 2026-09-14 워크로그

## Phase 4 — 부하 테스트 시나리오 설계

### 분석 & 질문

- `refactor_plan.md` Phase 4의 첫 TODO("부하 테스트 시나리오 설계, 랭킹 조회/동시 참가·기록 갱신 우선")를 처리 대상으로 확인.
- 관련 코드 분석: `participation.service.ts`의 `getChallengeRank`(랭킹 조회, 정렬 방향과 인덱스 컬럼 순서 불일치 가능성 — `refactor_plan.md` §1-7), `ParticipationService.update`(락 없는 read-modify-write, `refactor_plan.md` §1-2에서 실측 필요로 이월된 항목), 인증 흐름(`user`/`auth` 컨트롤러), 엔드포인트 계약(`participation.controller.ts`, `challenge.controller.ts`).
- 리포지토리에 부하 테스트 도구(k6/artillery 등)가 아직 없음을 확인.
- **질문**: 이번 세션에서 "시나리오 문서만" 작성할지, "문서 + 실행 가능한 스크립트"까지 만들지 `AskUserQuestion`으로 확인 → **"시나리오 문서만 작성"** 선택 (도구 선정/스크립트/실측은 다음 TODO로 이월).

### 변경사항 요약

| 파일 | 내용 |
|---|---|
| `anything/load_test_plan.md` (신규) | 랭킹 조회 / 동시 참가·기록 갱신 시나리오 설계 문서 — 우선순위 근거, 대상 환경, 시딩 계획, 인증 처리, 시나리오 상세, 측정 지표, 성공/실패 기준 초안, 이번 문서에서 다루지 않는 것(도구/스크립트/실측) 명시 |
| `anything/refactor_plan.md` | Phase 4 첫 TODO 체크 + 완료 메모 |

### 커밋 해시

- 코드 커밋: `19148a8` (Phase 4: 부하 테스트 시나리오 설계 문서 작성)

### 검증 결과

- `npm run build` 통과.
- 소스 코드(`src/`) 변경 없는 문서 전용 작업이라 유닛 테스트/`/code-review`는 해당 사항 없음(생략 사유를 사용자에게 사전 고지).

### 예외사항 / 남겨둔 것

- 부하 테스트 실행 도구 선정(k6/artillery/autocannon 등), 실제 스크립트 작성, 실측, EXPLAIN 분석, 커서 기반 페이지네이션 검토, 원자적 갱신 방식 최종 결정은 모두 Phase 4의 다음 TODO들로 이월.
- 동시 기록 갱신(패턴 B: 동일 유저-챌린지 참가 로우에 대한 동시 PATCH)은 정상 트래픽 패턴이 아니라 포함 여부를 문서에서 "실행 단계에서 결정"으로 열어둠 — `load_test_plan.md` §5-2 참고.

---

## Phase 4 — 랭킹 조회 부하 테스트 실행 + EXPLAIN 실측

### 분석 & 질문

- 다음 TODO("실측 후 슬로우 쿼리 EXPLAIN 확인")를 처리 대상으로 확인. `load_test_plan.md`에서 이미 k6로 방향을 정한 상태.
- 실행에 필요한 선행 결정 3가지를 `AskUserQuestion`으로 확인: (1) k6 설치 방법 → brew, (2) 대량 시딩 방식 → SQL bulk insert, (3) slow query log 활성화 방식 → `docker-compose.yml`에 `command` 영구 추가.
- k6 설치(`brew install k6` v2.2.0), `docker-compose.yml`에 slow query log(`long_query_time=0.05`) 추가 후 컨테이너 재기동.
- 시딩: `POST /user`/`POST /user/login`으로 실 유저 20명 생성 + score형/count형 챌린지 각 1개 생성 + 전원 참가(`setup_users.sh`) — 랭킹 endpoint가 참가자만 조회 가능하기 때문. 필러 참가 데이터는 랭킹 쿼리가 `user` 관계를 select하지 않는다는 점을 근거로 `user_id=NULL`인 5만 건×2챌린지(총 10만 건)를 SQL로 직접 insert(`seed_participation.py`).
- k6 스크립트(`rank_query.js`)로 `GET /participation/challenge/:id/rank`에 ramping-vus(0→100, 100초) 부하.

### 변경사항 요약

| 파일 | 내용 |
|---|---|
| `docker/docker-compose.yml` | MariaDB에 slow query log 설정 추가 (`long_query_time=0.05`) |
| `anything/loadtest/setup_users.sh` (신규) | 부하 테스트용 유저/챌린지 시딩 + 참가 처리 스크립트 |
| `anything/loadtest/seed_participation.py` (신규) | 필러 Participation 대량 생성 SQL 출력 스크립트 |
| `anything/loadtest/rank_query.js` (신규) | 랭킹 조회 k6 부하 테스트 스크립트 |
| `anything/load_test_plan.md` | §9 "실측 결과" 섹션 추가 (EXPLAIN 분석, offset 비용 실측, 커넥션 풀 반증 실험) |
| `anything/refactor_plan.md` | 해당 TODO 체크 + 완료 메모 |
| `.gitignore` | `anything/loadtest/tokens.json`(실 JWT), `seed_participation.sql`(생성 데이터) 제외 |

### 핵심 발견

1. **filesort 가설 기각**: `EXPLAIN`/`ANALYZE FORMAT=JSON`으로 확인한 결과, `(challenge_id, score, created_at)` 인덱스를 `ORDER BY score DESC, created_at DESC` 쿼리가 InnoDB 역방향 스캔으로 그대로 활용 — filesort 없음. `refactor_plan.md` §1-7에서 실측 전 우려했던 내용은 사실이 아니었음.
2. **진짜 병목 = offset 스캔 비용**: 같은 쿼리를 offset만 바꿔 격리 측정 — offset 0: 0.065ms → offset 40000: 42.8ms. k6 100 VU 부하 시 HTTP p95=1.75s(임계값 500ms 초과), slow query log에 4,582건(전체 요청의 87.5%) 기록.
3. **커넥션 풀 가설 반증**: "TypeORM 기본 풀(10)이 HTTP p95를 DB 쿼리 시간보다 훨씬 부풀린다"는 가설을 세우고 `app.module.ts`에 `extra: { connectionLimit: 100 }`을 임시 추가해 동일 조건 재측정 → p95 1.75s→2.1s, max 2.17s→4.7s, slow 쿼리 8,982건으로 **오히려 악화**. 풀 대기가 아니라 DB 자체의 동시 스캔 처리 한계가 원인임을 확인하고 가설 기각. 코드는 실험 후 diff 없이 원복.

### 커밋 해시

- 코드 커밋: `a7fecdb` (Phase 4: 랭킹 조회 부하 테스트 실행 + EXPLAIN 실측)

### 검증 결과

- `npm run build` 통과.
- `src/` 최종 변경 없음(커넥션 풀 실험은 원복 확인, `git diff src/app.module.ts` 무출력) — 유닛 테스트/코드리뷰 해당 없음.
- 시딩 스크립트는 실제 실행으로 end-to-end 검증(20명 가입/로그인/참가, 10만 건 insert, k6 실행, EXPLAIN 확인 전부 성공).

### 예외사항 / 남겨둔 것

- 로컬 DB에 이번 실측으로 생긴 필러 데이터(챌린지 410/411, 참가 10만 건, 테스트 유저 20여 명)를 정리하지 않고 남겨둠 — 사용자 확인, 다음 부하 테스트(동시 참가/기록 갱신)에서 재사용 예정.
- "커서 기반 페이지네이션 검토" TODO는 사용자와 논의 끝에 "상위 100위 캡 + 본인 순위 별도 조회" 방식으로 대체 결정(진행 중, 별도 커밋 예정).
