# 2026-09-07 작업 기록 — 마이그레이션 범위 제외 결정 + Phase 1-2 null 가드 추가

> `anything/refactor_plan.md` 계획 범위 조정(Phase 3 제외)과, Phase 1-2 첫 항목(`challenge.author`/`feed.user`/`participation.user`·`challenge` null 가드)을 진행한 기록.

---

## 1. 분석 & 질문

### 마이그레이션(Phase 3) 범위 제외
- 원래 계획은 리팩토링 → MariaDB→Supabase PostgreSQL 마이그레이션 → 부하 테스트 순서였으나, 사용자가 마이그레이션 단계를 이번 계획에서 빼고 리팩토링(Phase 1, 2) 이후 곧바로 부하 테스트(Phase 4)로 넘어가기로 결정.
- `refactor_plan.md`에 2026-09-07 갱신 이력 추가, §2 우선순위 목록에서 "7 마이그레이션 주의사항" 제거, Phase 3 섹션은 "범위 제외, 미실행"으로 표시하되 항목 자체는 참고용으로 남김.

### null 가드 분석
- **근본 원인**: `Challenge.author`, `Feed.user`/`challenge`, `Participation.user`/`challenge`가 전부 `@ManyToOne(..., { onDelete: 'SET NULL' })`인데, 엔티티 타입은 non-null(`User`, `Challenge`)로 선언되어 있어 컴파일러가 null 케이스를 못 잡음.
- grep으로 실제 `.id` 역참조 지점을 전수 확인 → 정확히 4곳: `response-challenge.dto.ts:21`, `response-feed.dto.ts:16-17`, `challenge.service.ts`의 update/delete(83, 116행), `feed.service.ts`의 update/delete(103, 127행). `Participation`은 서비스/DTO 어디서도 `.user.id`/`.challenge.id`를 직접 읽지 않아 실제 크래시 지점 없음(타입만 부정확).
- **실제 트리거 가능성 확인**: `UserService`에 삭제 로직 자체가 없고, `Challenge`/`Feed` 삭제도 `softDelete()`만 사용(물리 삭제 아님) → `onDelete: 'SET NULL'`은 물리 DELETE에서만 발동하므로, 현재 API 경로로는 이 null 상태가 절대 만들어지지 않음. 회원 탈퇴(하드 삭제) 기능이 추후 추가될 때를 대비한 선제적 수정으로 성격을 규정.
- 사용자가 짚은 두 가지 오해를 정정하며 논의:
  - "TypeORM이 조회 시 null 관계는 배제하는 거 아닌가?" → 아니다. `relations: [...]`는 LEFT JOIN이라 관계값이 null이어도 원본 row는 그대로 반환됨. `deleted_at IS NULL` 같은 명시적 `where` 조건만 자동 필터링됨.
  - "null !== userId로 자동으로 권한 비교가 막히는 거 아닌가?" → 의도한 최종 동작(작성자 없으면 아무도 접근 불가)은 맞지만, JS는 `null.id`를 `undefined`로 조용히 넘기지 않고 그 자리에서 `TypeError`를 던져 비교문 자체에 도달하지 못함. `?.`/`!x ||` 형태의 명시적 가드가 필요.
- **해결 방향 확정**: 엔티티 타입을 `T | null`로 정정해 TypeScript가 4개 지점을 컴파일 에러로 강제 검출하게 하고, 각 지점에 옵셔널 체이닝/가드 추가. DTO도 응답 계약(API contract) 관점에서 nullable로 맞춤(프론트엔드가 "작성자 없음" 케이스를 인지할 수 있어야 함).
- `ParticipationService.update`의 score/challenge_count 원자적 처리는 계획서 자체에 "실제 반영은 Phase 4와 함께 재검토"로 명시되어 있어 이번엔 검토만 하고 코드 변경은 보류.

---

## 2. 변경 사항 요약

| 파일 | 변경 내용 |
|---|---|
| `anything/refactor_plan.md` | Phase 3(PostgreSQL 마이그레이션) 범위 제외 반영, Phase 1-2 null 가드 항목 완료 처리 |
| `src/challenge/entity/challenge.entity.ts` | `author: User` → `author: User \| null` |
| `src/feed/entity/feed.entity.ts` | `user: User` → `User \| null`, `challenge: Challenge` → `Challenge \| null` |
| `src/participation/entity/participation.entity.ts` | `user`/`challenge` 동일하게 `\| null` 반영(타입 정합성 목적, 실제 역참조 지점은 없음) |
| `src/challenge/dto/response-challenge.dto.ts` | `author_id: number` → `number \| null`, `entity.author?.id ?? null` |
| `src/feed/dto/response-feed.dto.ts` | `user_id`/`challenge_id` 동일 패턴 적용 |
| `src/challenge/challenge.service.ts` | `update`/`delete`의 작성자 비교를 `!challenge.author \|\| challenge.author.id !== userId`로 변경 |
| `src/feed/feed.service.ts` | `update`/`delete`의 작성자 비교를 `!feed.user \|\| feed.user.id !== userId`로 변경 |
| `src/feed/feed.service.spec.ts` | 타입 변경으로 발생한 컴파일 에러 4곳(`feeds[0/1].user.id`, `.challenge.id`)에 non-null assertion(`!`) 추가 — 테스트 픽스처는 항상 값이 있음을 알고 있는 위치라 단언 처리 |

`anything/refactor_plan.md` Phase 1-2 첫 항목 체크리스트 완료 처리.

---

## 3. 검증 결과

- `npm run build` → 성공
- `npx tsc --noEmit -p tsconfig.json` → 에러 0개
- `npm test` → 10 suites / 72 tests 전부 통과
- `/code-review` 스킬(medium) 실행 → findings 0건. 검증 내역: 전수 grep으로 놓친 미가드 접근부 없음 확인, `ResponseParticipationDto`는 `user`/`challenge` 미참조라 영향 없음 확인, DB 레벨 nullable FK 전제가 기존 마이그레이션에 이미 반영되어 있음 확인.

---

## 4. 예외 사항 / 남겨둔 것

- Phase 1-2의 나머지 항목(`findByTitle` sentinel(0) 패턴 제거, `ParticipationService` score/count 원자적 처리 검토, `app.module.ts`/`typeorm.datasource.ts` DataSource 설정 통합)은 이번 세션에서 분석만 하고 아직 코드 변경은 진행하지 않음.
- 회원 탈퇴(하드 삭제) 기능은 현재 존재하지 않으며 이번 계획 범위에도 없음 — 이번 null 가드는 그 기능이 추가될 미래를 대비한 선제 조치. 실제 탈퇴 기능이 추가되면 이 가드들의 동작(조회는 `author_id: null`로 정상 노출, 수정/삭제는 403)을 다시 검증할 것.
