# 2026-09-16 워크로그

## Phase 4 — `ParticipationService.create`의 중복 참가 check-then-act race 최종 해결

### 분석 & 질문

- `refactor_plan.md` Phase 4의 마지막 미체크 TODO("`ParticipationService.create`의 중복 참가 check-then-act race 검토")를 처리 대상으로 확인 — 2026-09-15 score/challenge_count 원자성 분석 중 발견해 별도 항목으로 분리해뒀던 것.
- `analyze` 단계에서 `findOne`(사전 체크) → `create`+`save`(삽입) 사이에 원자성이 없고, `participation` 테이블에 `(user_id, challenge_id)` DB 레벨 유니크 제약이 없음을 마이그레이션 파일(`1766221023728-Init.ts`, `1766475886412-ModifyChallengeFK.ts`) 직접 확인으로 재검증. 같은 프로젝트의 `Challenge.title`/`Feed.title`은 이미 DB 유니크 제약으로 중복을 막는 선례가 있음을 확인.
- `plan` 단계에서 두 갈림길을 `AskUserQuestion`으로 확인:
  - 해결 방식: **DB 복합 유니크 인덱스 + 중복키 에러 캐치**(비관적 락 대비 title 컬럼과 컨벤션 일치, 구현 단순) 선택.
  - 검증 방식: **e2e `Promise.all` 동시 요청 테스트**(k6 인프라 없이 재현 가능) 선택.
- 마이그레이션 실행 전 로컬 DB를 직접 조회해 `(user_id, challenge_id)`가 non-null인 기존 중복 로우가 없음을 확인(부하 테스트 잔여 데이터는 `user_id IS NULL`이라 무관) — 안전하게 적용 가능함을 미리 검증.
- `migration:generate`가 이번 변경과 무관한 기존 스키마 drift(예: `feed.images` 컬럼 타입, FK 제약명 등, MariaDB의 `json`→`longtext+check` 내부 표현 차이로 인한 것으로 추정)까지 포함시켜서, 유니크 인덱스 추가 한 줄만 담도록 마이그레이션 파일을 수동으로 다시 작성.

### 변경사항 요약

| 파일 | 내용 |
|---|---|
| `src/participation/entity/participation.entity.ts` | `(user, challenge)` 복합 유니크 인덱스(`idx_unique_user_challenge`) 추가 |
| `src/migrations/1789528428691-AddParticipationUserChallengeUniqueIndex.ts` (신규) | 위 인덱스 생성/롤백 마이그레이션 (로컬 DB에 적용 완료) |
| `src/participation/participation.service.ts` | `create()`: `save()`를 try/catch로 감싸 `QueryFailedError`를 잡음 — `error.driverError.code === 'ER_DUP_ENTRY'`이고 `driverError.sqlMessage`에 `idx_unique_user_challenge`가 포함된 경우에만 기존과 동일한 `ConflictException("이미 참가중입니다.")`로 변환(다른 유니크 제약 위반과 혼동 방지). 메시지 리터럴은 `ALREADY_PARTICIPATING_MESSAGE` 상수로 추출 |
| `src/participation/participation.service.spec.ts` | 중복키 에러(해당 인덱스) → 409, 중복키 에러(다른 인덱스) → 그대로 전파, 무관한 에러 → 그대로 전파, 3케이스 단위 테스트 추가 |
| `test/app.e2e-spec.ts` | `Promise.all`로 동일 유저가 같은 챌린지에 동시 참가 요청 2건을 보내 하나는 201, 하나는 409가 되는지 확인하는 테스트 추가 |
| `anything/refactor_plan.md` | 해당 TODO 체크 + 완료 메모, 후속 TODO 2건 추가 |

### 코드리뷰에서 발견해 함께 수정한 것

`/code-review` 실행 결과 7건 지적, 직접 재현/검증 후 판단:

- **바로 수정(이번 diff 내 하드닝)**:
  - 중복키 판별이 `error`에 대한 unsafe cast(`error.code`, 비공식으로 복사된 속성에 의존)였던 것을 → TypeORM이 공식으로 노출하는 `error.driverError`로 교체.
  - `ER_DUP_ENTRY`만 보고 무조건 매칭하던 것을 → `driverError.sqlMessage`에 `idx_unique_user_challenge`가 포함된 경우로 한정. 실제 MariaDB 에러 메시지 형식(`Duplicate entry '1-2' for key 'idx_unique_user_challenge'`)을 로컬 DB에 수동 INSERT로 직접 재현해 확인.
  - `"이미 참가중입니다."` 리터럴 중복 기재를 상수로 추출.
- **별도 TODO로 분리(다른 모듈의 기존 버그, 이번 범위 밖)**:
  - `UserService.signUp()`의 이메일 중복가입 check-then-act 레이스(같은 구조, 미보호).
  - `Challenge.title`/`Feed.title` 생성 시 동일 계열 중복 제목 레이스(같은 구조, 미보호).
- **기록만 남기고 범위 제외**:
  - "새 유니크 인덱스가 soft-delete를 고려하지 않아, 참가 취소를 soft-delete로 구현하면 재참가가 영구히 막힐 수 있다" — 현재 `Participation`엔 soft-delete를 호출하는 코드가 전혀 없어(참가 취소 기능 미구현) 재현 불가능한 잠재 버그. 사용자와 논의 결과 hard-delete 전환은 선택지가 아님(이 프로젝트가 soft-delete를 쓰는 이유 자체가 로우 증가 시 영구 삭제 회피)을 확인, 향후 해결 후보로 (a) 생성 컬럼 트릭, (b) 재참가 시 `restore()` 패턴을 논의해 `refactor_plan.md`에 기록.
  - 마이그레이션에 사전 중복 데이터 정리 스텝이 없다는 지적은 실행 전 로컬 DB에 중복이 없음을 이미 확인했으므로 변경 불필요로 판단.

### 커밋 해시

- 코드 커밋: `135e967` (Phase 4: participation 중복 참가 check-then-act race를 DB 유니크 인덱스로 해결)

### 검증 결과

- `npm run build` 통과.
- 유닛 테스트 95개 전체 통과(`npm test`), e2e 59개 전체 통과(`npm run test:e2e`).
- 동시 참가 e2e 테스트를 실제 로컬 MariaDB로 총 8회 반복 실행 — 매번 정확히 201/409로 갈림(레이스가 안정적으로 재현되고 수정됨을 확인).
- 부하 테스트(k6)는 사용자와 사전 합의로 생략 — e2e `Promise.all`로 대체.
- `/code-review` 1회 실행, 발견 7건 중 3건 반영, 4건은 별도 TODO 또는 기록으로 분리.

### 예외사항 / 남겨둔 것

- `UserService.signUp()` 이메일 중복가입 레이스, `Challenge.title`/`Feed.title` 중복 제목 레이스는 `refactor_plan.md`에 새 TODO로 등록, 이번 커밋에 미포함.
- soft-delete와 새 유니크 인덱스의 잠재 충돌(참가 취소 기능 도입 시)은 아직 재현 불가능해 TODO 등록 없이 기록만 남김 — 참가 취소 기능을 실제로 만들 때 먼저 풀어야 함.
- 이것으로 `refactor_plan.md` Phase 4가 완전히 종료됨(원래 계획 "여기까지만 진행" + 이번 후속 발견 항목 2건은 새 TODO로 남음, Phase 5로 이어짐).

## Phase 5 — CORS 설정 추가

### 분석 & 질문

- `/todo`에서 Phase 4 나머지 2건(User 이메일 중복가입 레이스, Challenge/Feed 제목 중복 레이스)과 Phase 5 CORS 항목 중 어느 걸 먼저 할지 사용자에게 물었고, Phase 5가 더 간단하다는 판단(레이스 조건 재현/검증이 필요 없는 단순 설정 변경)에 사용자가 동의해 Phase 5 CORS부터 진행.
- `analyze` 단계에서 `main.ts`에 CORS 설정이 전혀 없음을 확인, `frontend-pratice`(로컬 확인) Vite 설정에 커스텀 포트가 없어 기본 `5173`으로 추정, 인증이 쿠키가 아닌 Authorization 헤더 기반이라 `credentials` 옵션이 불필요함을 확인. 추가로 `app.module.ts`의 `ConfigModule.forRoot`가 `envFilePath` 지정 없이 `.env`만 로드하고 `package.json` 스크립트에도 `.env.dev`/`.env.prod`를 선택하는 로직이 없어, 이 두 파일이 현재 미사용(과거 EC2/CodeDeploy 파이프라인의 흔적으로 추정)임을 발견.
- `plan` 단계에서 origin 값을 `AskUserQuestion`으로 확인: **환경변수(`FRONTEND_ORIGIN`) 신설 + 로컬 기본값** 선택 — 사용자가 "프론트 포트가 나중에 바뀌면 환경변수만 갱신하면 되는 거잖아?"로 방향을 먼저 확인해줌.

### 변경사항 요약

| 파일 | 내용 |
|---|---|
| `src/main.ts` | `app.enableCors({ origin: process.env.FRONTEND_ORIGIN })` 추가 + 부팅 시 `FRONTEND_ORIGIN` 미설정이면 즉시 에러로 실패하는 fail-fast 검증 추가 |
| `.env` / `.env.dev` / `.env.prod` (gitignore, 커밋 없음) | `FRONTEND_ORIGIN` 추가 |
| `CLAUDE.md` | 필수 환경변수 목록에 `FRONTEND_ORIGIN` 추가 |
| `anything/refactor_plan.md` | Phase 5 CORS TODO 체크 + 완료 메모 |

### 코드리뷰에서 발견해 함께 수정한 것

`/code-review` 실행 결과 2건 지적, 둘 다 재현 확인 후 반영:

- `node_modules/cors/lib/index.js:41`(`if (!options.origin || options.origin === '*')`)을 직접 확인해, `FRONTEND_ORIGIN`이 비어있으면 `enableCors`가 origin을 `'*'`로 열어버려 CORS 제한이 무력화됨을 재현 — 부팅 시 값이 없으면 즉시 에러로 죽도록 검증 추가해 수정.
- `CLAUDE.md` 필수 환경변수 목록에 신규 변수가 누락된 문서 갭 — 목록에 추가해 수정.

### 커밋 해시

- 코드 커밋: `1262e1f` (Phase 5: CORS 설정 추가 (FRONTEND_ORIGIN 환경변수 기반))

### 검증 결과

- `npm run build` 통과.
- 유닛 테스트 95개 전체 통과(`npm test`).
- e2e는 생략 — CORS는 브라우저 전용 정책이라 supertest 기반 e2e로는 의미 있게 검증되지 않아 이번 변경의 회귀 가능성과 무관하다고 판단.
- `/code-review` 1회 실행, 발견 2건 모두 반영.

### 예외사항 / 남겨둔 것

- 실제 `frontend-pratice`(Vite) 개발 서버를 띄워 브라우저에서 CORS 통과를 직접 확인하지는 않음 — 프론트엔드 개발 자체가 아직 시작 전이라 이번 범위 밖.
- Phase 4 나머지 2건(User 이메일 중복가입 레이스, Challenge/Feed 제목 중복 레이스)은 이번 작업에 포함하지 않고 그대로 남겨둠.

## Phase 5 — 업로드 이미지 정적 서빙 설정

### 분석 & 질문

- Phase 5의 두 번째이자 마지막 항목(CORS는 앞서 완료). 사용자가 "Phase 5 나머지 처리하자"로 지정.
- `analyze` 단계에서 `main.ts`에 정적 서빙 설정이 전혀 없음, `@nestjs/serve-static` 미설치·`@nestjs/platform-express`는 이미 존재함을 확인. `common/util.ts`의 멀터 저장 경로 계산(`path.join(__dirname, '..', 'uploads/${folder}')`)과 `main.ts` 기준 `path.join(__dirname, 'uploads')`가 dev/prod 양쪽에서 일치할 것으로 계산상 추정, 설계 갈림길 없다고 판단해 `plan` 단계는 게이트 없이 바로 진행.
- 사용자가 구현 방식 확인 중 "절대 경로 대신 어떤 가상의 경로를 셋팅해서 접근한다는 건가?" 질문 — `/uploads` prefix가 물리 경로와 분리된 가상 URL 매핑이라는 점을 설명.
- **구현 후 실 서버 검증 중 analyze의 경로 추정이 틀렸음을 발견**: `npm run start:dev`는 ts-node가 아니라 tsc watch로 `dist/src/`에 컴파일 후 실행하는 방식이라(`nest-cli.json`에 `rootDir` 미지정이라 `dist` 밑에 `src/` 구조가 그대로 보존됨) 실제 `__dirname`은 `dist/src/`였음. 다만 이 사실이 결론(멀터 경로와 서빙 루트 경로가 항상 일치)에는 영향 없었음 — 두 계산 모두 같은 `__dirname` 기준 상대 깊이를 쓰기 때문. 처음 curl 검증에서 404가 나온 이유는 이 추정 오류가 아니라, 과거 다른 실행 방식으로 생성된 `src/uploads/feed/`의 stale 테스트 파일을 잘못 사용했기 때문(실제 서빙 루트는 `dist/src/uploads/feed/`) — 원인 파악 후 올바른 위치에 테스트 파일을 둬서 재검증, 200 확인.

### 변경사항 요약

| 파일 | 내용 |
|---|---|
| `src/main.ts` | `NestFactory.create<NestExpressApplication>(AppModule)`로 타입 변경, `app.useStaticAssets(UPLOADS_ROOT_DIR, { prefix: '/uploads' })` 추가 |
| `src/common/util.ts` | `UPLOADS_ROOT_DIR` export 상수 신설(`path.join(__dirname, '..', 'uploads')`), `createFolder`/`storage` 내부 계산을 이 상수 기준으로 교체 |
| `anything/refactor_plan.md` | 해당 TODO 체크 + 완료 메모 — Phase 5 두 항목 모두 완료 |

### 코드리뷰에서 발견해 함께 판단한 것

`/code-review` 실행 결과 2건 지적:

- **바로 수정**: `main.ts`와 `util.ts`가 uploads 루트 경로를 각자 독립적으로 계산하고 있어(공유 상수 없음) 향후 디렉터리 구조가 바뀌면 두 계산이 조용히 어긋나 이미지가 전부 404날 수 있다는 지적 — `UPLOADS_ROOT_DIR` 공유 상수로 통합해 근본적으로 어긋날 수 없게 수정.
- **코드 변경 없이 기록만**: "정적 서빙이 인증 없이 전체 공개되고, `feed.service.ts:132`의 `softDelete`가 DB 로우만 지우고 디스크 파일은 지우지 않아 soft-delete된 피드 이미지도 URL만 알면 영구 조회 가능하다"는 지적 — `feed.service.ts:132` 직접 확인해 사실 재현. 다만 이건 `plan` 단계에서 "업로드 파일 접근 권한/인증 제어... 보안 강화는 프로젝트 방침상 범위 밖"으로 이미 명시적으로 범위 제외하고 사용자 승인을 받은 내용과 동일해, 새로 발견된 이슈가 아니라 기존 합의를 재확인한 것으로 판단 — 코드 변경 없이 `refactor_plan.md`에 향후 재검토 조건(인증 필요해지면 `JwtAuthGuard` 걸린 전용 컨트롤러 라우트로 전환)만 기록.

### 커밋 해시

- 코드 커밋: `f9b4bea` (Phase 5: 업로드 이미지 정적 서빙 설정 추가)

### 검증 결과

- `npm run build` 통과, 유닛 테스트 95개 전체 통과(`npm test`).
- `npm run start:dev`로 실 서버 기동 후 수동 검증: 실제 업로드 경로(`dist/src/uploads/feed/`)에 둔 파일 → `GET /uploads/feed/파일명` 200(내용 일치), 존재하지 않는 파일 → 404, path traversal 시도(`/uploads/../main.js`, URL 인코딩 포함) → 404. `UPLOADS_ROOT_DIR` 리팩터 이후 재검증도 200으로 통과.
- e2e는 생략 — 기존 e2e 부트스트랩이 CORS 등 다른 transport 설정도 복제하지 않는 선례와 일관되게, 정적 서빙도 supertest보다 실 서버 수동 검증이 더 적합하다고 판단.
- `/code-review` 1회 실행, 발견 2건 중 1건 반영(경로 중복 제거), 1건은 기존 합의 재확인으로 코드 변경 없이 기록.

### 예외사항 / 남겨둔 것

- 정적 서빙이 인증 없이 공개되는 것과 soft-delete된 이미지의 영구 노출 문제는 의도적으로 미해결 — 실제 프론트엔드 연동 중 문제가 되면 인증 필요한 전용 라우트로 전환 검토.
- 이것으로 `refactor_plan.md` Phase 5(CORS + 정적 서빙)가 완전히 종료됨. Phase 4 나머지 2건(User 이메일 중복가입 레이스, Challenge/Feed 제목 중복 레이스)은 여전히 미완료로 남아있음.

## Phase 4 — `UserService.signUp()`의 이메일 중복가입 check-then-act race 해결

### 분석 & 질문

- `/todo`에서 `refactor_plan.md` Phase 4의 미완료 항목 중 다음 순서인 "`UserService.signUp()`의 이메일 중복가입 check-then-act 레이스 검토"(participation 중복 참가 레이스 수정(2026-09-16) 중 code-review로 발견해뒀던 항목)를 선택.
- `analyze` 단계에서 `signUp()`의 `save()` 호출에 예외 처리가 없음을 확인. `email` 컬럼엔 이미 DB 유니크 인덱스가 존재하지만(초기 마이그레이션에서 TypeORM 자동생성 해시명 `IDX_e12875dfb3b1d92d7d7c5377e2`로 생성), participation처럼 사람이 읽을 수 있는 이름이 아니라는 차이를 발견. 로컬 MariaDB의 실제 `Duplicate entry` 에러 메시지가 인덱스명을 그대로 노출함은 2026-09-16 worklog에서 이미 확인해둔 사실 재사용.
- `plan` 단계에서 두 갈림길을 `AskUserQuestion`으로 확인:
  - 인덱스명 처리: **마이그레이션으로 `idx_unique_email`로 rename**(해시 리터럴을 코드에 박아넣지 않기 위함) 선택.
  - 작업 범위: **User만 우선 처리**, Challenge/Feed 제목 중복 레이스는 별도 TODO로 유지 선택.

### 변경사항 요약

| 파일 | 내용 |
|---|---|
| `src/user/entity/user.entity.ts` | `@Column({ unique: true })` 제거, 엔티티 레벨 `@Index('idx_unique_email', ['email'], { unique: true })`로 전환(participation 컨벤션과 일치) |
| `src/migrations/1789535350539-RenameUserEmailUniqueIndex.ts` (신규) | 자동생성 해시 인덱스명을 `idx_unique_email`로 rename (로컬 DB에 적용 완료) |
| `src/user/user.service.ts` | `signUp()`: `save()`를 try/catch로 감싸 `QueryFailedError`를 잡음 — `driverError.code === 'ER_DUP_ENTRY'`이고 `driverError.sqlMessage`에 `idx_unique_email`이 포함된 경우에만 기존과 동일한 `ConflictException("이미 존재하는 이메일입니다.")`로 변환. 메시지 리터럴은 `ALREADY_EXISTING_EMAIL_MESSAGE` 상수로 추출 |
| `src/user/user.service.spec.ts` | 중복키 에러(해당 인덱스) → 409, 중복키 에러(다른 인덱스) → 그대로 전파, 무관한 에러 → 그대로 전파, 3케이스 단위 테스트 추가 |
| `test/app.e2e-spec.ts` | `Promise.all`로 동일 이메일 동시 가입 요청 2건을 보내 하나는 201, 하나는 409가 되는지 확인하는 테스트 추가 |
| `anything/refactor_plan.md` | 해당 TODO 체크 + 완료 메모 |

### 코드리뷰에서 발견해 함께 판단한 것

`/code-review` 실행(8각도 병렬 탐색) 결과 8건 지적, 직접 재현/검증 후 판단:

- **바로 수정**: 마이그레이션이 `DROP INDEX` + `CREATE UNIQUE INDEX` 2단계 raw SQL이라, MariaDB DDL의 암묵적 커밋 특성상 원자적이지 않고(중간에 프로세스가 죽으면 `email` 유니크 제약이 아예 없는 상태로 남을 수 있음) participation 수정 때보다 더 위험한 지적이었음 — 단일 `ALTER TABLE user RENAME INDEX ... TO ...`(MariaDB 10.5.2+ 지원, 메타데이터만 바꾸는 원자적 연산)로 교체. 마이그레이션을 revert 후 재작성, 재실행해 정상 적용 확인.
- **재현 시도 후 반증**: "`TransactionalTestDataSource`가 모든 Repository를 테스트당 하나의 공유 커넥션/트랜잭션에 rebind하므로, 새로 추가한 e2e 동시 가입 테스트가 실제로는 진짜 크로스커넥션 레이스가 아니라 순차 직렬화된 것이라 새 catch 경로(유니크 인덱스 위반)가 아니라 기존 사전 체크(`findOneByEmail`) 경로만 타고 있을 수 있다"는 지적 — 메커니즘 자체는 정확한 관찰이었으나(코드 직접 확인으로 공유 커넥션 사실 확인), 실제로 새 catch 경로 안에 임시 `console.error` 프로브를 넣고 해당 e2e 테스트를 5회 반복 실행한 결과 매번 프로브가 찍혀 새 catch 경로가 실제로 히트함을 확인 — 공유 커넥션이어도 두 요청의 비동기 인터리빙이 레이스를 재현하기에 충분했음. 프로브는 검증 후 제거.
- **범위 밖으로 판단(기록만)**:
  - participation과 거의 동일한 `MysqlDriverError`/`ER_DUP_ENTRY` 판별 블록의 코드 중복 — `plan` 단계에서 이미 User만 처리하기로 범위를 확정했으므로, 공유 헬퍼로 추출하면 participation 파일까지 건드리게 되어 범위 밖. Challenge/Feed 제목 중복 레이스까지 처리하게 되면(총 3곳 중복) 그때 재검토.
  - 인덱스명 문자열이 엔티티/마이그레이션/서비스 3곳에 리터럴로 중복된다는 지적 — participation의 `idx_unique_user_challenge`도 동일한 구조라 기존 컨벤션과 일치, 새로운 문제가 아님.
  - 정상 경로에서 `findOneByEmail` 사전 체크가 불필요한 추가 쿼리라는 지적 — participation의 `create()`도 동일한 사전 체크+catch 이중 방어 구조라 기존 컨벤션과 일치.
  - `main.ts`의 `bootstrap()` 미체결 프로미스, `PORT!` non-null assertion 등 2건 — 이번 diff와 무관한 기존 코드(Phase 5 CORS 작업 당시 추가된 코드)라 범위 밖으로 판단, 코드 변경 없음.
  - 마이그레이션 적용 전 DB에서는 새 catch 로직이 무력화(구 인덱스명으로 실패)된다는 지적 — 이 프로젝트의 모든 마이그레이션 기반 스키마 변경에 공통되는 특성(배포 전 마이그레이션 실행이 전제)이라 이번 변경이 새로 만든 리스크가 아님.

### 커밋 해시

- 코드 커밋: `c4a8263` (Phase 4: UserService.signUp() 이메일 중복가입 check-then-act race 해결)

### 검증 결과

- `npm run build` 통과.
- 유닛 테스트 98개 전체 통과(`npm test`), e2e 60개 전체 통과(`npm run test:e2e`).
- 동시 가입 e2e 테스트에 임시 프로브를 넣어 5회 반복 실행 — 매번 새 catch 경로(유니크 인덱스 위반 처리)가 실제로 히트함을 확인 후 프로브 제거, 재검증 통과.
- 부하 테스트(k6)는 해당 없음 — participation 때와 동일하게 e2e `Promise.all`로 레이스 재현.
- `/code-review` 1회 실행(8각도), 발견 8건 중 1건 반영(마이그레이션 원자성), 1건은 직접 재현 시도로 반증, 나머지 6건은 범위 밖/기존 컨벤션 일치로 판단.

### 예외사항 / 남겨둔 것

- `Challenge.title`/`Feed.title` 생성 시 동일 계열 중복 제목 레이스는 `refactor_plan.md`에 그대로 미완료 TODO로 남아있음 — 이번 커밋에 미포함.
- `MysqlDriverError` 판별 블록이 이제 participation/user 두 곳에 중복 — Challenge/Feed까지 같은 패턴을 적용하게 되면 공유 헬퍼 추출을 재검토할 것.
- `main.ts`의 `bootstrap()` 미체결 프로미스, `PORT!` non-null assertion은 이번 범위 밖으로 남겨둠(기존 코드, Phase 5 당시 추가분).

## Phase 4 — `Challenge.title`/`Feed.title` 생성 시 동일 계열 중복 제목 레이스 보호 검토

### 분석 & 질문

- `/todo`에서 `refactor_plan.md` Phase 4의 마지막 미완료 항목("`Challenge.title`/`Feed.title` 생성 시 동일 계열 중복 제목 레이스 보호 검토")을 선택. 이걸로 Phase 4의 모든 미완료 항목 처리 완료.
- `analyze` 단계에서 `ChallengeService`/`FeedService`의 `create()`뿐 아니라 `update()`도 동일한 check-then-act 구조(제목 변경 시 `findByTitle` 사전 체크 후 `save()`)를 갖고 있음을 확인 — 원래 TODO 문구는 "생성 시"로 한정. `findByTitle`이 `withDeleted: true`로 soft-delete 레코드까지 포함해 중복을 판단하는 것이, participation과 달리 DB 유니크 인덱스와 이미 일관되어 있어(soft-delete 포함) participation류의 "유니크 인덱스가 soft-delete를 고려하지 않는" 부가 문제는 challenge/feed엔 해당하지 않음을 확인. `challenge.title`/`feed.title` 유니크 인덱스도 User email 때와 동일하게 TypeORM 자동생성 해시명임을 마이그레이션 파일에서 확인.
- `plan` 단계에서 세 갈림길을 `AskUserQuestion`으로 확인:
  - 범위: **create + update 모두** 처리(동일 구조가 그대로 남아있으면 반쪽짜리 해결로 판단될 가능성) 선택.
  - 마이그레이션: challenge/feed 두 테이블의 인덱스 rename을 **마이그레이션 파일 하나로 묶음** 선택.
  - 공유 헬퍼: participation/user/challenge/feed까지 4곳(update 포함 시 최대 8곳)째 반복되는 `MysqlDriverError`/`ER_DUP_ENTRY` 판별 블록을 **이번에도 계속 복붙**하기로 선택(공유 헬퍼 추출 보류).

### 변경사항 요약

| 파일 | 내용 |
|---|---|
| `src/challenge/entity/challenge.entity.ts` | `@Column({ unique: true })` 제거, 엔티티 레벨 `@Index('idx_unique_challenge_title', ['title'], { unique: true })`로 전환 |
| `src/feed/entity/feed.entity.ts` | 동일하게 `idx_unique_feed_title`로 전환 |
| `src/migrations/1789536813417-RenameChallengeFeedTitleUniqueIndex.ts` (신규) | challenge/feed 두 테이블의 자동생성 해시 인덱스명을 각각 원자적 `ALTER TABLE ... RENAME INDEX`로 rename (로컬 DB에 적용 완료) |
| `src/challenge/challenge.service.ts` | `create()`/`update()`가 공유하는 private `saveOrThrowDuplicateTitle()` 헬퍼 신설 — `save()`를 try/catch로 감싸 `QueryFailedError`(`ER_DUP_ENTRY` + `sqlMessage`에 `for key 'idx_unique_challenge_title'` 포함)만 기존과 동일한 `ConflictException("중복된 제목입니다.")`로 변환 |
| `src/feed/feed.service.ts` | 동일 패턴, `idx_unique_feed_title` 매칭 |
| `src/challenge/challenge.service.spec.ts` | create/update 각각에 중복키 에러(해당 인덱스) → 409, 중복키 에러(다른 인덱스) → 그대로 전파, 무관한 에러 → 그대로 전파, 3케이스씩 유닛 테스트 추가 |
| `src/feed/feed.service.spec.ts` | 동일하게 create/update 각 3케이스 추가 |
| `test/app.e2e-spec.ts` | challenge/feed 각각 `Promise.all`로 동일 제목 동시 생성 요청 2건을 보내 하나는 201, 하나는 409가 되는지 확인하는 테스트 추가 |
| `anything/refactor_plan.md` | 해당 TODO 체크 + 완료 메모 — Phase 4 전체 종료 |

### 코드리뷰에서 발견해 함께 판단한 것

`/code-review` 실행 결과 10건 지적, 직접 재현/검증 후 판단:

- **바로 수정**: 중복키 판별이 `sqlMessage.includes(인덱스명)` bare substring 매칭이라, title 값 자체가 우연히 인덱스명 문자열을 포함하면(이론상 가능한 30자 이내 문자열) 무관한 유니크 위반을 오탐할 수 있다는 지적 — `for key '인덱스명'`까지 포함한 패턴으로 강화(challenge/feed 두 파일만; participation/user는 이번 범위 밖이라 동일 약점이 남아있음을 인지하고 기록만 남김). 매칭 강화 후 `for key '<challenge 인덱스명>'` 자체가 30자(title 최대 길이)를 넘는 길이라 title 값만으로는 절대 오탐할 수 없음을 길이 계산으로 확인.
- **재현 시도 후 반증**: User 때와 동일하게 "`TransactionalTestDataSource`의 공유 커넥션 때문에 새 e2e 레이스 테스트가 실제로는 기존 사전 체크 경로만 탈 수 있다"는 지적 — challenge/feed 양쪽 새 catch 경로에 임시 프로브를 넣고 5회 반복 실행, 매번 히트함을 확인 후 제거.
- **범위 밖으로 판단(기록만)**:
  - `common/util.ts`의 멀터 파일명이 `Date.now()`(밀리초)만 쓰고 랜덤성이 없어 동시 업로드 시 파일명 충돌 가능하다는 지적 — 사실 확인(코드 직접 확인). 업로드 로직 전반의 별도 이슈로 이번 제목 레이스 범위 밖. 이번에 추가한 feed 동시 생성 e2e 테스트가 두 요청 모두 동일한 내용(`Buffer.from('test')`)을 업로드해 설령 파일명이 충돌해도 무해함을 확인 — 테스트 신뢰성엔 문제 없음.
  - `feed.service.ts`의 이미지 경로가 `feed/파일명`으로 반환되는데 실제 정적 서빙 경로는 `/uploads/feed/파일명`이라 프론트가 그대로 URL을 만들면 404난다는 지적 — 재현 확인. Phase 5 정적 서빙 작업(2026-09-16) 당시 만들어진 기존 버그로 이번 diff와 무관, 범위 밖.
  - `main.ts`의 정적 서빙이 `JwtAuthGuard`를 우회해 인증 없이 전체 공개된다는 지적, `FRONTEND_ORIGIN` 외 다른 필수 환경변수는 fail-fast 검증이 없다는 지적 — 둘 다 Phase 5 당시 이미 논의/기록된 기존 이슈, 이번 diff와 무관해 범위 밖.
  - `MysqlDriverError` 판별 블록이 이제 4개 서비스에 반복된다는 지적, 테스트의 `makeDupEntryError` 헬퍼가 3개 spec 파일에 중복된다는 지적 — `plan` 단계에서 이미 사용자 확인 하에 "계속 복붙"으로 결정.
  - 마이그레이션 적용 전 DB에서는 새 catch 로직이 무력화된다는 지적 — User 때와 동일하게 이 프로젝트의 마이그레이션 기반 스키마 변경에 공통되는 특성으로 새로운 리스크 아님.

### 커밋 해시

- 코드 커밋: `0b63f84` (Phase 4: Challenge/Feed title check-then-act race 해결)

### 검증 결과

- `npm run build` 통과.
- 유닛 테스트 110개 전체 통과(`npm test`), e2e 62개 전체 통과(`npm run test:e2e`).
- 동시 생성 e2e 테스트(challenge/feed) 각각에 임시 프로브를 넣어 5회 반복 실행 — 매번 새 catch 경로가 실제로 히트함을 확인 후 프로브 제거, 재검증 통과.
- 매칭 강화(`for key '...'` 패턴) 후 유닛/e2e 재실행 — 로컬 실 MariaDB의 실제 에러 메시지 포맷과 일치함을 확인(전체 통과).
- `/code-review` 1회 실행, 발견 10건 중 1건 반영(substring 매칭 강화), 1건은 직접 재현 시도로 반증, 나머지 8건은 범위 밖/기존 컨벤션 일치로 판단.

### 예외사항 / 남겨둔 것

- `MysqlDriverError` 판별 블록의 substring 매칭 강화(`for key '...'`)는 challenge/feed에만 적용 — participation/user에도 동일 약점(이론상 훨씬 좁은 확률)이 남아있음, 후속 정리 후보.
- `feed.service.ts`의 이미지 경로(`feed/파일명`)와 실제 정적 서빙 경로(`/uploads/feed/파일명`) 불일치는 Phase 5 때부터의 기존 버그로 확인됐으나 이번 범위 밖 — 프론트엔드 연동 중 실제로 문제 되면 새 TODO로 등록 검토.
- `common/util.ts`의 멀터 파일명 밀리초 충돌 가능성도 이번 범위 밖 — 업로드 로직을 다시 다룰 때 재검토.
- 이것으로 `refactor_plan.md` Phase 4가 완전히 종료됨(부하 테스트 + 이번 레이스 조건 정리 2건까지 모두 완료).
